# v0.1-shipped

- initial public ship of `robot-fleet-exception-board`
- C# / ASP.NET robotics operator surface for mission failures, override packets, and redeploy posture
- live routes: `/`, `/fleet-lane`, `/mission-failures`, `/override-posture`, `/verification`, `/docs`
- synthetic sample data only
- Pages deploy with `robots.kineticgain.com`
